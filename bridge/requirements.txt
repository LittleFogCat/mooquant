# mooquant Python bridge dependencies
#
# Install:
#   pip install -r requirements.txt
#
# xtquant is distributed by QMT client, or: pip install xtquant
xtquant>=1.0

# PyTorch is OPTIONAL. Only needed for ML strategies and training.
# Without torch, rule-based strategies work normally.
# Install: pip install torch
torch>=2.0

# numpy is required by indicators and features
numpy>=1.20

# pytest for unit tests
pytest>=7.0
